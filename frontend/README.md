# frontend — Vue 3 前端

Vue 3 + Vite + TypeScript 前端 SPA。

```
src/
  views/        ← 页面组件
  components/   ← 通用组件
  stores/       ← Pinia 状态管理
  api/          ← 后端 API 调用
  types/        ← TypeScript 类型定义
  router/       ← 路由配置
```

构建产物输出到 dist/，拷贝到 ai-app 的 static/ 目录。
